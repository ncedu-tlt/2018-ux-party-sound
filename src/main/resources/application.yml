spring:
  jackson:
    default-property-inclusion: non_null
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:postgresql://ocassio.asuscomm.com:8080/console-app?user=nc2018&password=uKCc7uMnt8VzF9CwyyrSc3Wcwq7cGdDh}
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

server:
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain,application/javascript,text/css

